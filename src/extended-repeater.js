module.exports = function repeater(/*str, options*/) {        
    throw "Not implemped"
};
  
/*
if (options.separator === undefined) {
        options.separator = "+";
        }
        
      if (options.additionSeparator === undefined) {
        options.additionSeparator = "";
        }
       
      if (options.repeatTimes === undefined) {
        options.repeatTimes = 1;
        }
      
      if (options.additionRepeatTimes === undefined) {
        options.additionRepeatTimes = 1;
        options.additionSeparator = ""
        }
      
      if (options.addition === undefined) {
        options.addition = "";
        }
      
      let test = (str + (options.addition + options.additionSeparator).repeat(options.additionRepeatTimes) + options.separator).repeat(options.repeatTimes);
      
      if (options.repeatTimes == 1) {
        return test.slice(0, test.length - options.separator.length - options.additionSeparator.length);
        }
      return test.slice(0, test.length - options.separator.length);
*/